services:
  frontend:
    image: react-next-shadcn-chat-app
    build:
      context: ./client
      target: dev
      dockerfile: Dockerfile
    volumes:
        - .:/app
        - /app/node_modules
        - /app/.next
    ports:
      - "3000:3000"